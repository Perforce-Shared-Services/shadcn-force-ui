/**
 * Calendar 16 block — SINGLE SWAP POINT for the block's own decorative glyph.
 *
 * Raw inline SVG imported from `@material-symbols/svg-400` (Material
 * Symbols, Rounded style, outline = FILL 0) via the webpack `?raw` rule,
 * injected as trusted HTML by the component. Same convention as
 * `calendar.icons.ts` / `button.icons.ts`.
 *
 * Maps to the registry's `lucide-react` `Clock2Icon` (a clock face) by
 * MEANING, not glyph name — `schedule` is Material Symbols' clock glyph.
 */
import schedule from '@material-symbols/svg-400/rounded/schedule.svg?raw';

/** Leading glyph inside each time input (Start Time / End Time). */
export const CALENDAR_16_CLOCK_SVG = schedule;
