/**
 * InputOTPSeparator icon — single swap point.
 *
 * Raw inline SVG from `@material-symbols/svg-400` (Material Symbols, Rounded
 * style, outline = FILL 0) via the webpack `?raw` rule, injected as a real
 * `<svg>` through `[innerHTML]` (sanitizer-trusted). Maps the registry's
 * `IconPlaceholder materialSymbols="remove"` by meaning. To swap the icon,
 * change the import here only.
 */
import removeSvg from '@material-symbols/svg-400/rounded/remove.svg?raw';

export const INPUT_OTP_SEPARATOR_SVG = removeSvg;
