/**
 * Native-select icon set — SINGLE SWAP POINT.
 *
 * Same glyph meaning as `select`'s trigger chevron (down disclosure), so the
 * two components read consistently, but imported separately per the
 * one-swap-point-per-component convention (see button.icons.ts).
 */
import chevronDown from '@material-symbols/svg-400/rounded/keyboard_arrow_down.svg?raw';

/** Raw inline SVG for the decorative disclosure chevron overlay. */
export const NATIVE_SELECT_ICON_SVG = chevronDown;
