/**
 * Questionnaire icon set — SINGLE SWAP POINT (see `port-shadcn-component` §9).
 *
 * One glyph: the checkmark shown in a `multiple` (checkbox-style) item's
 * choice indicator when checked. A `radio`-style item's indicator uses a
 * plain filled dot (no icon needed), matching upstream's own
 * `QuestionnaireChoiceIndicatorDot` / `...Check` split.
 */
import check from '@material-symbols/svg-400/rounded/check.svg?raw';

export const QUESTIONNAIRE_CHOICE_CHECK_SVG = check;
